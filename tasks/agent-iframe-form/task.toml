schema_version = "1.4"

[task]
name = "browserbase/agent-iframe-form"

[metadata]
suite = "agent"
task_id = "agent-iframe-form"

[environment]
network_mode = "public"

[agent]
timeout_sec = 900

[verifier]
timeout_sec = 900
environment_mode = "shared"
