schema_version = "1.4"

[task]
name = "browserbase/agent-all-recipes"

[metadata]
suite = "agent"
task_id = "agent-all-recipes"

[environment]
network_mode = "public"

[agent]
timeout_sec = 900

[verifier]
timeout_sec = 900
environment_mode = "shared"
